Active transition assets:

- /public/cavapendoli/models-bw.png
- /public/cavapendoli/models-b.png

Optional reserve asset (not currently used):

- /public/cavapendoli/models-a.png

Expected format:

- each image is 1536x1024
- each image is a 3x3 collage (8-9 models)

Rendering behavior (App-level prelude):

- on first load and public page transitions, a short sequence appears
- B/W sheet fades in first, then dissolves into the colored sheet
- sequence is soft, blurred, and non-blocking
- no static collage background is used anymore

Design guardrails:

- text and actions stay primary
- no social-feed look
- motion remains slow and minimal
